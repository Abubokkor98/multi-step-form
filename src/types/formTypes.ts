export type PersonalInfo = {
  fullName: string;
  email: string;
  phone: string;
};

export type AddressInfo = {
  street: string;
  city: string;
  zipCode: string;
};

export type AccountInfo = {
  username: string;
  password: string;
  confirmPassword: string;
};
